import { CreateBorrowInput } from '../../dto/borrow.dto';

export const mockGenre: CreateBorrowInput = {
  takenDate: undefined,
};

export const mockUpdateGenre: CreateBorrowInput = { takenDate: undefined };
